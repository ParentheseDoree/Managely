@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirige vers la page de connexion personnalisée
        Navigation.NavigateTo("/login", forceLoad: false);
    }
}
